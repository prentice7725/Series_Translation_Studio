{
  "name": "@sts/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "out/main/index.js",
  "scripts": {
    "predev": "pnpm --filter @sts/common build && pnpm --filter @sts/db build && pnpm --filter @sts/epub-core build",
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "lint": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@sts/common": "workspace:*",
    "@sts/db": "workspace:*",
    "@sts/epub-core": "workspace:*",
    "@vitejs/plugin-react": "^4.4.1",
    "electron": "^35.2.1",
    "electron-vite": "^3.1.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "zod": "^3.24.3",
    "zustand": "^5.0.3"
  },
  "devDependencies": {
    "@types/react": "^19.1.2",
    "@types/react-dom": "^19.1.3",
    "vite": "^6.3.4"
  }
}
